# Dependencies for agent, rag, tools, agent_eval, and api services.
# Pinned incrementally per project rule S23.
-r base.txt

# Component 6 (FastAPI services: edge_service.py, agent_service.py).
fastapi==0.136.1
starlette==1.0.0
uvicorn==0.46.0
uvloop ; sys_platform != "win32"  # uvloop is unix-only; not pinned on Windows.
watchfiles==1.1.1
websockets==16.0
httptools==0.7.1
click==8.3.3
colorama==0.4.6
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
certifi==2025.11.12

# Component 4 (RAG): vector store + dense embeddings.
chromadb==0.5.23
sentence-transformers==3.4.1
scikit-learn==1.8.0
numpy==2.3.5
