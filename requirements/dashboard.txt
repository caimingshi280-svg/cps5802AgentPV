# Dependencies for the operator dashboard (Component 6).
# Pinned incrementally per project rule §23 — packages are added here only
# when the corresponding component begins implementation.
-r base.txt

# Component 6 (Streamlit dashboard) — installed.
streamlit==1.57.0
altair==6.1.0
pyarrow==24.0.0
blinker==1.9.0
pydeck==0.9.2
gitpython==3.1.50
watchdog==6.0.0
python-multipart==0.0.27
tenacity==9.1.4
toml==0.10.2
itsdangerous==2.2.0
cachetools==7.1.1

# httpx is used optionally if the dashboard needs to call services directly;
# in MVP it reads the JSONL event stream directly so httpx is not required.
